.section.sign-up-box
  .well.col-sm-8
    .row
      .brand-header
        = image_tag 'logomark_trans.png', class: 'main-logo'
        %p.opensit opensit
    %h1 Namaste! Get your beautiful, free meditation journal today.
    .row
      .left.col-md-offset-3.col-md-6
        = simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), html: { honeypot: true } ) do |f|
          = f.error_notification
          -# = f.input :username, placeholder: 'Choose a username', label: false, input_html: { autocomplete: 'off' }
          = f.input :email, placeholder: 'Your email', label: false, input_html: { autocomplete: 'off' }
          = f.input :password, placeholder: 'Your password', label: false, input_html: { autocomplete: 'off' }
          = f.submit "Sign up", :class => 'btn btn-primary btn-lg cta pull-left'
          .sign_in_link
            Already signed up?
            %a{:href => new_user_session_path} Time to sign in!
    .row.col-md-12
      .our-community
        %h2 And become part of our awesome global community!
        -# - @users.each do |u|
        -#   .user-snippet
        -#     = sign_up_avatar(u)
        -#     .details
        -#       = u.display_name
        -#       %br/
        -#       = u.location

